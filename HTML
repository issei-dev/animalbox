<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ガチャ機能</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #F8F0F8; /* 淡いピンク */
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            text-align: center;
        }
        .container {
            background-color: #fff;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.1);
            width: 90%;
            max-width: 500px;
        }
        h1 {
            color: #E6A9C8; /* パステルピンク */
        }
        .points-display {
            font-size: 24px;
            margin-bottom: 20px;
            color: #555;
        }
        .points-display span {
            font-weight: bold;
            color: #8A2BE2; /* 紫 */
        }
        #gacha-button {
            padding: 15px 30px;
            font-size: 20px;
            color: #fff;
            background-color: #B0E0E6; /* パステルブルー */
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        #gacha-button:hover:not(:disabled) {
            background-color: #87CEEB;
        }
        #gacha-button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
        #result {
            margin-top: 30px;
            padding: 20px;
            background-color: #F0F8FF; /* 淡い水色 */
            border-radius: 10px;
            min-height: 150px;
        }
        #result-text {
            font-size: 20px;
            font-weight: bold;
            color: #4B0082; /* 濃い紫 */
        }
        #result-image {
            max-width: 150px;
            height: auto;
            border-radius: 10px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>ガチャ</h1>
        <div class="points-display">
            いまのえさポイント: <span id="points-count">100</span>
        </div>
        <button id="gacha-button" onclick="drawGacha()">ガチャをまわす (30ポイント)</button>
        <div id="result">
            <p id="result-text">ボタンを押して、どうぶつさんをゲットしよう！</p>
            <img id="result-image" style="display:none;">
        </div>
    </div>

    <script src="gacha.js"></script>
</body>
</html>
